import { createExpressApp } from '$tests/utils';

// TODO:
// Add tests
it('should return 400 status code when the body.price_id is not supported/valid', () => {
  expect(true).toEqual(false);
});

it('should return 200 status code with the checkout session url', () => {
  expect(true).toEqual(false);
});

// Note: Feel free to add more tests that you deem useful for ensuring the reliability of this endpoint.